{
  "name": "agfoods-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:sepolia": "npx hardhat run scripts/deploy-evm.js --network sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0",
    "ethers": "^6.8.0",
    "dotenv": "^16.0.0"
  }
}
